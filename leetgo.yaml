# This is the leetgo project level config, global config is at /home/yaniru/.config/leetgo/config.yaml
# For more details, please refer to https://github.com/j178/leetgo
language: zh
code:
  lang: cpp
  cpp:
    blocks:
      - name: beforeMarker
        template: |
          // TODO: complete this problem
leetcode:
  site: https://leetcode.cn
  credentials:
    from: browser
    browsers: [firefox]
editor:
  use: custom
  command: "nvim"
  args: "{{.CodeFile}} {{.DescriptionFile}}"
